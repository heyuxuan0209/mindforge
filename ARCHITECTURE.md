# 飞书多 Agent 协作系统架构

## 系统目标

让用户在飞书群里通过 @ 不同的 AI Bot，实现多模型协作开发，后台自动维护项目上下文文档。

## 核心架构

```
┌─────────────────────────────────────────────────────────────┐
│                        飞书群聊                              │
│  用户 → @Claude / @Codex / @Gemini                           │
└──────────────────┬──────────────────────────────────────────┘
                   │ Webhook 事件
                   ▼
┌─────────────────────────────────────────────────────────────┐
│                   Bot 服务 (Node.js)                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 1. 消息路由器                                        │   │
│  │    - 识别 @ 的 Bot                                   │   │
│  │    - 提取用户消息                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                   │                                          │
│  ┌────────────────▼──────────────────────────────────┐     │
│  │ 2. 上下文管理器                                     │     │
│  │    - 从飞书文档读取 brief/decisions/handoff         │     │
│  │    - 从群聊历史读取近期消息                         │     │
│  │    - 组装 Context Packet                            │     │
│  └─────────────────────────────────────────────────────┘   │
│                   │                                          │
│  ┌────────────────▼──────────────────────────────────┐     │
│  │ 3. Agent 调用器                                     │     │
│  │    - Claude API (中转商)                            │     │
│  │    - Codex API (中转商)                             │     │
│  │    - Gemini API                                     │     │
│  └─────────────────────────────────────────────────────┘   │
│                   │                                          │
│  ┌────────────────▼──────────────────────────────────┐     │
│  │ 4. 响应处理器                                       │     │
│  │    - 回复飞书群                                     │     │
│  │    - 提取关键决策                                   │     │
│  │    - 更新飞书文档                                   │     │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────┐
│               飞书文档（项目知识库）                          │
│  - brief.md         项目背景                                 │
│  - decisions.md     关键决策日志                             │
│  - current.md       当前产物状态                             │
│  - handoff.md       交接包（给下个 Agent）                    │
│  - review.md        Review 问题追踪                          │
│  - memory.md        长期记忆                                 │
│  - status.md        项目状态                                 │
└─────────────────────────────────────────────────────────────┘
```

## 技术栈

| 层级 | 技术选型 | 原因 |
|------|---------|------|
| Bot 服务 | Node.js + Express | 轻量、飞书 SDK 支持好 |
| 飞书 SDK | @larksuiteoapi/node-sdk | 官方 SDK |
| AI API 调用 | axios | 通用 HTTP 客户端，支持中转商 |
| 文档存储 | 飞书文档 API | 用户可见、可编辑、云端同步 |
| 配置管理 | .env | API Key、Webhook URL 等敏感信息 |
| 部署 | 本地 + ngrok | 开发阶段，后续可迁移到服务器 |

## 数据流

### 用户发消息

```
用户在群里：@Claude 帮我设计用户积分系统
    ↓
飞书 Webhook → Bot 服务
    ↓
1. 识别是 @Claude
2. 读取项目文档（brief + decisions + handoff）
3. 组装 prompt：
   [系统 prompt]
   项目背景：{brief.md}
   已确认决策：{decisions.md 最近 5 条}
   交接包：{handoff.md}
   
   [用户消息]
   帮我设计用户积分系统
    ↓
4. 调用 Claude API（中转商）
    ↓
5. 收到响应
    ↓
6. 分析响应内容：
   - 是否包含决策关键词？→ 追加到 decisions.md
   - 是否是方案产出？→ 更新 current.md
    ↓
7. 回复飞书群
```

### Agent 切换

```
用户：@Gemini 挑战一下上面的方案
    ↓
1. 识别是 @Gemini
2. 读取：
   - brief.md（项目背景）
   - current.md（Claude 刚才的方案）
   - 群聊最近 20 条消息（包括 Claude 的方案）
3. 组装 Reviewer prompt：
   你是 Reviewer，任务是挑战，不是帮忙。
   
   项目背景：{brief}
   被审查产物：{current}
   
   要求：
   - 最多 5 个问题
   - 按严重程度排序
   - 格式：[S0|S1|S2] 问题描述 -> 建议
    ↓
4. 调用 Gemini API
    ↓
5. 提取 Review Issues → 写入 review.md
    ↓
6. 回复飞书群
```

## 文档自动维护规则

| 触发条件 | 自动动作 | 目标文件 |
|---------|---------|---------|
| 消息包含"决定""确定""不考虑" | 提取决策，追加 | decisions.md |
| Agent 输出超过 500 字的方案 | 摘要存储 | current.md |
| 用户说"记住""重要" | 写入长期记忆 | memory.md |
| Review 输出问题列表 | 结构化存储 | review.md |
| 用户说"下一步""接下来" | 更新状态 | status.md |

## 配置文件结构

```
/Users/heyuxuan/feishu-multi-agent/
  ├── .env                    # API Key、Webhook 配置
  ├── package.json
  ├── src/
  │   ├── app.js              # Express 服务入口
  │   ├── handlers/
  │   │   ├── messageHandler.js    # 消息路由
  │   │   ├── contextManager.js    # 上下文组装
  │   │   └── documentManager.js   # 文档读写
  │   ├── agents/
  │   │   ├── claudeAgent.js       # Claude API 调用
  │   │   ├── codexAgent.js        # Codex API 调用
  │   │   └── geminiAgent.js       # Gemini API 调用
  │   └── utils/
  │       ├── feishuClient.js      # 飞书 SDK 封装
  │       └── documentParser.js    # 决策提取、文档更新
  └── ARCHITECTURE.md         # 本文档
```

## 部署步骤

1. **本地开发环境**
   ```bash
   cd /Users/heyuxuan/feishu-multi-agent
   npm install
   node src/app.js
   ```

2. **内网穿透**
   ```bash
   ngrok http 3000
   # 得到 https://xxx.ngrok.io
   ```

3. **飞书配置**
   - 创建 3 个机器人应用（Claude Bot、Codex Bot、Gemini Bot）
   - 配置 Webhook URL: https://xxx.ngrok.io/webhook
   - 配置权限：
     - 读取群消息
     - 发送群消息
     - 读写飞书文档

4. **创建项目群和文档**
   - 创建飞书群
   - 把 3 个 Bot 加入群
   - 创建飞书文档（一个多维表格或者文档，包含 7 个子文档）

## MVP 范围

第一版只做核心功能：

✅ **包含**
- 识别 @Claude / @Codex / @Gemini
- 调用对应的中转商 API
- 从飞书文档读取 brief + decisions
- 回复飞书群
- 手动触发文档更新（用户说"记住这个决策：XXX"）

❌ **不包含**
- 自动提取决策（需要 NLP 或 LLM 二次调用，复杂）
- 群聊历史读取（飞书 API 限制，需要遍历）
- 多项目支持（一个 Bot 只服务一个项目）

## 风险和限制

1. **飞书 API 限流**：每分钟 120 次调用，足够小团队使用
2. **文档读写延迟**：飞书文档 API 有 1-2 秒延迟
3. **中转商稳定性**：依赖你的中转商可用性
4. **成本**：每次对话都调用 API，按 token 计费

## 后续扩展

- 支持多项目（一个 Bot 服务多个群）
- 自动决策提取（用 Claude Haiku 做轻量分析）
- 群聊历史上下文（缓存近期消息）
- Web 管理后台（查看项目文档、统计 token 消耗）
